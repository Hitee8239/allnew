use test 
db.place.drop()
db.people.drop()

db.places.insert({
    name : "Broadway Center",
    url  : "bc.example.net"
})

db.places.insert({
    name : "Erin",
    place_id : db.places.findOne({name: "Broadway Center"})._id,
    url  : "bc.example.net/Erin"
})
